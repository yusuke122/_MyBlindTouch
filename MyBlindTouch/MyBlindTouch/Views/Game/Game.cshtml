@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model MyBlindTouch.Models.GameModel
<div class="banner">
    
    <table class="table">
    <thead>
        <tr>
                <th>
                    @{
                        /*
                        <h2 id="gametimeid">gametime=@ViewBag.gametime</h2>
                        <h2>questiontime=@ViewBag.questiontime</h2>
                        <h2>onoff=@ViewBag.soundonoff</h2>
                        <h2>gamesound=@ViewBag.gamesound</h2>
                        foreach (var item in Model.LevelList)
                        {
                        <p>@item</p>  
                        }//値が渡されていることが確認できた
                        */
                        /*
                         foreach (var item in Model.QuizStrCopy)
                        {
                         <h3>@item</h3>
                        }*///値が渡されていることが確認できた
                         //linqで指定されたステージの文字列のみ参照
                            //Model.LevelCopyとModel.QuizStrCopyを連結？
                            //linqで表示する文字列を抽出、抽出後json,jsで表示

                        var QuizStrDIct = new Dictionary<string, string>();
                        for (int i=0;i<Model.QuizStrCopy.Count;i++)
                        {
                            QuizStrDIct[Model.QuizStrCopy[i]] = Model.LevelCopy[i];
                        }
                        

                    }
            </th>
            <th></th>
        </tr>
    </thead>
    </table>

  <a asp-controller="Home" asp-action="Index"class="btn btn-defaultLayout">戻る</a>
</div>